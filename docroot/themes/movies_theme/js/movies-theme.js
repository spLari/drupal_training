/**
 * @file
 * movies_theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.moviesTheme = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
